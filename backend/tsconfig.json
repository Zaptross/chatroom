{
    "extends": "../tsconfig.base.json",
    "compilerOptions": {
        // Build options
        "outDir": "./build",
        "rootDir": "./src"
    },
    "include": ["src/**/*.ts"],
    "exclude": ["node_modules"]
}
